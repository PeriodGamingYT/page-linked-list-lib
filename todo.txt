- Instead of having *WithSize() functions with only InitPage*WithSize() and
having a boolean for whether or not the PageLinkedList contains
CustomSizeElements.

- Implement PageLinkedListPop() and PageLinkedListPopWithSize().
